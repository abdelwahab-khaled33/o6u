# PRD — منصة الامتحانات الجامعية
**المشروع:** مشروع تخرج — جامعة 6 أكتوبر
**الإصدار:** v1.0
**الحالة:** معتمد للتنفيذ (بعد تأكيد بروتوكول SSO مع تقنية المعلومات)

---

## 1. نظرة عامة (Overview)

منصة إلكترونية داخلية تُستخدم فعليًا بالجامعة، تتيح للدكاترة والمعيدين إنشاء امتحانات وكويزات إلكترونية، بحيث يحصل كل طالب على نسخة فريدة من الأسئلة مبنية على خلطة صعوبة ثابتة يحددها أستاذ المادة، مع تصحيح آلي فوري وحماية كاملة لسلامة عملية الامتحان.

## 2. المشكلة (Problem Statement)

الامتحانات الورقية أو الإلكترونية الحالية (إن وُجدت) لا توفر:
- توزيع أسئلة عادل يمنع الغش عبر نسخة موحدة لكل الطلبة.
- تصنيف الأسئلة حسب مستوى الصعوبة بشكل منظم يمكن أستاذ المادة من التحكم فيه.
- تصحيحًا آليًا فوريًا للأسئلة الموضوعية.
- سجل تدقيق (audit trail) موثوق يحمي حقوق الطالب والدكتور عند التظلّم.

## 3. الأهداف (Goals)

| الهدف | مقياس النجاح |
|---|---|
| توزيع عادل للأسئلة | كل طالب يحصل على نسخة فريدة بنفس مجموع الدرجة بالضبط |
| تقليل عبء التصحيح | تصحيح آلي فوري لـ 100% من أسئلة الاختيار من متعدد وصح/خطأ |
| منع الغش الأساسي | تسجيل كل محاولة نسخ/لصق أو خروج من الشاشة الكاملة بدون استثناء |
| الموثوقية أثناء الامتحان | عدم فقدان أي إجابة أو وقت متبقٍ عند انقطاع الاتصال |
| قابلية التدقيق | كل تعديل إداري على امتحان مسجَّل بمن قام به ومتى |

### خارج النطاق حاليًا (Out of Scope — v1)
- الأسئلة المقالية أو التي تحتاج تصحيحًا يدويًا.
- عرض الدرجة أو الإجابات للطالب داخل المنصة.
- أداء الامتحان من خارج معمل الجامعة (لا يوجد دعم "امتحان من المنزل" في v1).
- تصحيح تكيّفي أو خوارزميات IRT لمعايرة الصعوبة إحصائيًا.
- ورك فلو اعتماد (لا يحتاج امتحان المعيد موافقة الدكتور قبل النشر).

## 4. المستخدمون والأدوار (Personas)

| الدور | يقدر يعمل إيه |
|---|---|
| **الدكتور** | إنشاء/نشر امتحانات وكويزات لمادته، إدارة بنك الأسئلة، رؤية النتائج والتقارير |
| **المعيد** | نفس صلاحيات الدكتور على مستوى الإنشاء والنشر المباشر (بدون اعتماد)، بالإضافة لإنشاء كويزات بسيطة |
| **الطالب** | تسجيل دخول عبر بيانات الجامعة، أداء الامتحان في المعمل فقط، بدون رؤية الدرجة أو الإجابات بعد التسليم |
| **Admin** | صلاحية كاملة على تعديل أي امتحان **قبل بدايته فقط**؛ بعد بدء الامتحان يُقفل التعديل تمامًا حتى عن الـ Admin |

## 5. متطلبات المنتج الوظيفية (Functional Requirements)

### 5.1 تسجيل الدخول والصفحة الرئيسية
- FR-1: تسجيل الدخول عبر ربط مباشر بنظام SSO/API الخاص بالجامعة (بدون كلمة مرور منفصلة للطلبة والهيئة التدريسية).
- FR-2: صفحة رئيسية منظمة تختلف محتوياتها حسب الدور (Doctor / TA / Student / Admin).

### 5.2 بنك الأسئلة
- FR-3: الدكتور/المعيد يضيف سؤال اختيار من متعدد أو صح/خطأ، ويحدد مستوى صعوبته (سهل/متوسط/صعب).
- FR-4: النظام يعرض نسبة الإجابة الصحيحة التاريخية لكل سؤال بعد أول استخدام له (لمساعدة الدكتور على مراجعة تصنيف الصعوبة).

### 5.3 بناء الامتحان
- FR-5: عند إنشاء امتحان، يحدد المنشئ خلطة الصعوبة (عدد الأسئلة لكل مستوى + الدرجة لكل مستوى).
- FR-6: النظام يمنع نشر الامتحان إذا كان بنك الأسئلة لا يحتوي على عدد كافٍ من الأسئلة لأي مستوى مطلوب، ويوضح النقص بالضبط.
- FR-7: يدعم النظام نوعين: "امتحان" و"كويز بسيط" بنفس آلية البناء والتوزيع.
- FR-8: نشر امتحان المعيد يتم مباشرة دون الحاجة لموافقة الدكتور.

### 5.4 أداء الامتحان (الطالب)
- FR-9: عند بدء الطالب الامتحان، يولّد النظام نسخة فريدة (أسئلة مسحوبة عشوائيًا من كل مستوى صعوبة حسب الخلطة).
- FR-10: نص كل سؤال يُحفظ كنسخة ثابتة (snapshot) وقت التوليد، بحيث لا تتأثر ورقة الطالب بأي تعديل لاحق على السؤال في البنك.
- FR-11: الوقت المتبقي يُحسب من السيرفر وليس من متصفح الطالب.
- FR-12: عند انقطاع اتصال الطالب، يستأنف الامتحان تلقائيًا بنفس الإجابات المحفوظة ونفس الوقت المتبقي الصحيح.
- FR-13: يتم قفل الشاشة الكاملة (fullscreen) ومنع النسخ/اللصق أثناء الامتحان.
- FR-14: أي خروج من وضع fullscreen أو محاولة نسخ/لصق يُسجَّل بالتوقيت في سجل مخالفات مرتبط بمحاولة الطالب.
- FR-15: بعد التسليم، تُقفل المحاولة نهائيًا، ولا تظهر للطالب أي درجة أو إجابات صحيحة/خاطئة.

### 5.5 المراقبة والإدارة
- FR-16: لوحة حية للمعيد/الدكتور تعرض حالة كل طالب في المعمل لحظيًا (بدأ / لم يبدأ / سلّم / عدد المخالفات المسجلة).
- FR-17: الـ Admin يقدر يعدّل أي بيانات في امتحان لم يبدأ بعد فقط. أي محاولة تعديل بعد البدء تُرفض من النظام تلقائيًا.
- FR-18: كل عملية تعديل إدارية (إنشاء/تعديل/حذف) تُسجَّل في سجل تدقيق يحتوي القيمة قبل وبعد ومن قام بها ومتى.

### 5.6 التصحيح والدرجات
- FR-19: تصحيح آلي فوري لكل الأسئلة عند التسليم.
- FR-20: مجموع درجة الامتحان النهائي ثابت ومتساوٍ لكل الطلبة رغم اختلاف الأسئلة، لأن الدرجة محسوبة على مستوى الخلطة لا على السؤال الفردي.
- FR-21: النتائج تظهر فقط للدكتور/الـ Admin عبر تقرير مخصص، ولا تصل للطالب داخل المنصة إطلاقًا.

## 6. المتطلبات غير الوظيفية (Non-Functional Requirements)

| المتطلب | التفصيل |
|---|---|
| الأداء | يجب أن يتحمل النظام دخول 20-30 طالب لبدء الامتحان في نفس اللحظة بالضبط دون تأخير محسوس |
| الاستضافة | يعمل بالكامل على سيرفر داخل شبكة الجامعة، بدون اعتماد على أي خدمة سحابية خارجية |
| الأمان | تقييد الوصول لواجهة أداء الامتحان على نطاق IP معمل الجامعة فقط |
| الموثوقية | عدم فقدان أي إجابة مُدخَلة مهما حدث انقطاع في الشبكة أو الجهاز |
| قابلية التتبع | كل حدث مؤثر (بدء/تسليم/مخالفة/تعديل إداري) مسجَّل بتوقيت دقيق |
| تجربة الاستخدام | واجهة بسيطة ومنظمة بنظام ألوان مريح (درجات برتقالي، رصاصي فاتح، أبيض) بعيدًا عن الألوان النمطية لواجهات الذكاء الاصطناعي |

## 7. الافتراضات والقيود (Assumptions & Constraints)

- **افتراض غير مؤكد بعد:** توفر SSO/API فعلي من الجامعة يسمح بالتحقق من هوية الطالب/عضو هيئة التدريس. يجب تأكيده مع قسم تقنية المعلومات قبل مرحلة الربط الفعلي.
- المعمل المستخدم للامتحان مزوَّد بإشراف بشري مباشر — الحماية التقنية (fullscreen/نسخ-لصق) مكمِّلة له وليست بديلة عنه.
- لا يوجد اتصال إنترنت خارجي مطلوب لتشغيل الامتحان (نظام محلي بالكامل داخل شبكة الجامعة).

## 8. المخاطر (Risks)

| الخطر | الأثر | التخفيف |
|---|---|---|
| عدم توفر API حقيقي من الجامعة | تعطّل الربط بالكامل | طبقة `AuthProvider` منفصلة تسمح بالعمل بحساب يدوي/استيراد مؤقتًا |
| ضغط الدخول المتزامن وقت بدء الامتحان | تأخير أو فشل في توليد النسخ | اختبار حمل مسبق + استخدام Redis لحالة الجلسات الحية |
| نقص بنك الأسئلة عن الخلطة المطلوبة | فشل نشر الامتحان وقت الحاجة | تحقق إلزامي من كفاية البنك (FR-6) قبل النشر، مبكرًا وليس وقت الامتحان |
| تجاوز طالب للحماية التقنية (fullscreen/نسخ-لصق) | غش غير مكتشف تقنيًا | الاعتماد الأساسي على الإشراف البشري + سجل المخالفات كدليل داعم |

## 9. المراحل والجدول الزمني (Milestones)

مبنية على خطة التنفيذ التفصيلية في وثيقة التصميم التقني المرفقة سابقًا:
1. تأكيد بروتوكول SSO مع الجامعة
2. هيكلة المشروع (Backend + DB + Docker)
3. بنك الأسئلة
4. بناء الامتحان والخلطات
5. محرك التوليد والأداء (النسخة الفريدة، autosave، التايمر)
6. التصحيح الآلي + سجل التدقيق
7. لوحة المراقبة الحية
8. الواجهة الأمامية الكاملة
9. التقييد الأمني (IP، HTTPS الداخلي)
10. اختبار حمل الدخول المتزامن
11. التوثيق والنشر على سيرفر الجامعة

## 10. أسئلة مفتوحة (Open Questions)

- ما البروتوكول الدقيق لـ SSO الجامعة (SAML / OAuth2 / LDAP)؟
- هل هناك مواصفات محددة لسيرفر الجامعة (RAM / CPU / نظام التشغيل) يجب التصميم حسبها؟
- هل هناك سياسة رسمية لمدة الاحتفاظ بسجلات المخالفات وسجل التدقيق؟
