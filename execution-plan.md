# خطة تنفيذ منصة الامتحانات الجامعية

## 1. الهدف والنطاق

إنشاء منصة امتحانات داخلية لجامعة 6 أكتوبر تتيح للدكتور والمعيد إنشاء امتحانات وكويزات موضوعية، وتولّد نسخة أسئلة فريدة لكل طالب وفق خلطة صعوبة ثابتة. تعمل المنصة داخل شبكة الجامعة، وتدعم في النسخة الأولى أسئلة الاختيار من متعدد والصح والخطأ فقط.

في مرحلة التطوير يستخدم النظام بيانات وحسابات وهمية من خلال `MockAuthProvider`. يبقى الربط الحقيقي مع نظام الجامعة معزولًا خلف واجهة `AuthProvider` حتى يمكن استبداله لاحقًا بعد تأكيد بروتوكول SSO.

### المتطلبات المحدثة

- تحمل 10,000 طالب متصلين ويؤدون امتحانًا في الوقت نفسه، بما في ذلك موجة بدء الامتحان.
- تصحيح تلقائي وتسجيل الدرجة النهائية لكل طالب عند التسليم أو انتهاء الوقت.
- إتاحة النتائج للدكتور والمعيد المخول للمادة وAdmin فقط. لا يرى الطالب درجته أو الإجابات.
- تصدير درجات الامتحان المغلق إلى ملف Excel يحتوي اسم المادة في أعلى الملف، ثم اسم الطالب والرقم الجامعي والدرجة.
- حفظ الإجابات تلقائيًا واستئناف المحاولة بنفس الإجابات والوقت المتبقي بعد انقطاع الاتصال.

## 2. القرارات المعمارية

| الطبقة | الاختيار | الغرض |
|---|---|---|
| الواجهة | React + TypeScript + Vite + Tailwind CSS | واجهات الطالب وهيئة التدريس والإدارة |
| API | NestJS + TypeScript | وحدات منظمة وصلاحيات وواجهات REST وWebSocket |
| قاعدة البيانات | PostgreSQL + Prisma | بيانات المستخدمين والامتحانات والمحاولات والدرجات وسجل التدقيق |
| الجلسات والحالة الحية | Redis Cluster | التخزين المؤقت وrate limiting وpub/sub والطوابير |
| الوقت الحقيقي | Socket.io + Redis adapter | لوحة المراقبة والأحداث الحية بين خوادم متعددة |
| المهام الخلفية | Queue workers | تصدير Excel والتصحيح أو الإحصاءات غير الفورية |
| التشغيل المحلي | Docker Compose | تطوير وتشغيل الخدمات محليًا |
| التشغيل الإنتاجي | Kubernetes أو خوادم API متعددة خلف موازن تحميل | دعم 10,000 اتصال وعدم ربط النظام بخادم واحد |
| موازن التحميل | Nginx أو HAProxy | HTTPS داخلي وتوزيع الطلبات وتقييد IP المعامل |

```text
React SPA
    | HTTPS و WebSocket
Load Balancer
    |
API instances متعددة بلا حالة محلية
    |-- PostgreSQL عبر PgBouncer
    |-- Redis Cluster
    |-- Socket.io عبر Redis adapter
    `-- Queue workers لتصدير Excel والمهام الخلفية
```

> Docker Compose مناسب للتطوير فقط. الإنتاج المطلوب لتحمل 10,000 طالب يحتاج تشغيلًا موزعًا واختبار حمل ناجح على عتاد الجامعة الفعلي.

## 3. بنية المشروع

استخدام Monorepo منظم بالتقسيم التالي:

```text
apps/
  api/                 # NestJS API
  web/                 # React SPA
packages/
  shared/              # Types وZod schemas مشتركة
  config/              # إعدادات مشتركة
infra/
  docker/              # Docker Compose وNginx للتطوير
  k8s/                 # ملفات النشر الإنتاجية عند توافر البنية
docs/                  # التوثيق وقرارات التصميم
```

## 4. نموذج البيانات

### الجداول الأساسية

- `users`: الرقم الجامعي، الاسم، البريد، والدور العام: `ADMIN` أو `DOCTOR` أو `TA` أو `STUDENT`.
- `courses`: اسم المادة وكودها.
- `course_staff`: ربط أعضاء هيئة التدريس بالمقررات وتحديد دورهم في المادة.
- `course_students`: ربط الطلاب بالمقررات المؤهلين لأداء امتحاناتها.
- `questions`: نص السؤال، النوع، الاختيارات، الإجابة الصحيحة، الصعوبة، المنشئ، وعدد الاستخدامات ونسبة الإجابات الصحيحة.
- `exams`: الامتحان أو الكويز، المادة، المدة، المواعيد، الحالة، والمنشئ.
- `exam_slots`: خلطة الامتحان: مستوى الصعوبة وعدد الأسئلة ودرجة كل سؤال.
- `exam_attempts`: محاولة الطالب وحالتها ووقت البداية والتسليم ودرجتها النهائية وIP الجهاز.
- `attempt_questions`: نسخة ثابتة من نص السؤال واختياراته وقت التوليد، وإجابة الطالب وصحتها والدرجة المكتسبة.
- `violation_logs`: محاولات النسخ واللصق والخروج من الشاشة الكاملة وتغيير التبويب.
- `audit_log`: التعديلات الإدارية والقيم قبل وبعد وفاعل العملية وتوقيتها.
- `exam_results_exports`: طلبات تصدير Excel وحالة المهمة ومسار الملف ووقت انتهاء الصلاحية.

### قواعد البيانات الحرجة

- فرض قيد فريد على `(exam_id, student_id)` في `exam_attempts` لمنع أكثر من محاولة للطالب.
- تخزين `question_snapshot` كاملًا داخل `attempt_questions` وعدم الاعتماد على السؤال الحي في البنك.
- إنشاء فهارس على `exam_attempts(exam_id, student_id)` و`exam_attempts(exam_id, status)` و`attempt_questions(attempt_id, order_index)` و`violation_logs(attempt_id, occurred_at)`.
- تنفيذ بدء المحاولة والتسليم النهائي داخل transactions.

## 5. الصلاحيات

| العملية | الدكتور | المعيد | Admin | الطالب |
|---|---:|---:|---:|---:|
| إدارة بنك أسئلة المادة | نعم | نعم ضمن المادة | نعم | لا |
| إنشاء ونشر امتحان | نعم | نعم | نعم قبل البدء | لا |
| متابعة الطلاب مباشرة | نعم | نعم | نعم | لا |
| رؤية النتائج | نعم لمادته | نعم لمادته | نعم | لا |
| تصدير Excel | نعم لمادته | نعم لمادته | نعم | لا |
| أداء الامتحان | لا | لا | لا | نعم |

تُطبّق هذه الصلاحيات داخل API من خلال RBAC والتحقق من ملكية المورد أو التكليف بالمادة. لا يكفي إخفاء الأزرار في الواجهة.

## 6. الوحدات الوظيفية

### 6.1 المصادقة والبيانات التجريبية

- تعريف واجهة `AuthProvider`.
- تنفيذ `MockAuthProvider` لتسجيل الدخول بالحسابات التجريبية.
- تجهيز seed data يشمل Admin ودكاترة ومعيدين وطلابًا ومقررات وبنك أسئلة متعدد الصعوبة.
- تنفيذ `UniversitySSOAuthProvider` لاحقًا بعد معرفة بروتوكول الجامعة، من دون تغيير وحدات الأعمال الأخرى.

### 6.2 المستخدمون والمقررات

- إدارة المقررات وتكليف أعضاء هيئة التدريس وتسجيل الطلاب.
- إرجاع لوحة رئيسية تختلف حسب الدور.
- منع الوصول إلى موارد مادة غير مسندة للمستخدم.

### 6.3 بنك الأسئلة

- إضافة وتعديل وحذف أسئلة MCQ وTrue/False.
- التحقق بـZod من صيغة الاختيارات والإجابة الصحيحة.
- تصنيف الأسئلة إلى سهل ومتوسط وصعب.
- عرض عدد الاستخدامات ونسبة الإجابة الصحيحة لكل سؤال بعد أول استخدام.

### 6.4 بناء الامتحان

- إنشاء امتحان أو كويز مع عنوان ومدة وموعد ومادة.
- تعريف `exam_slots` للخلطة: صعوبة وعدد أسئلة ودرجة لكل سؤال.
- فحص كفاية بنك الأسئلة قبل النشر وإظهار النقص لكل مستوى.
- منع تعديل الامتحان بعد نشره أو بعد بدء أول محاولة، بما في ذلك Admin.

### 6.5 محرك المحاولات

- بدء idempotent للمحاولة: إذا كانت للطالب محاولة جارية، تعاد المحاولة نفسها.
- اختيار الأسئلة عشوائيًا لكل مستوى صعوبة وتخزين snapshot داخل عملية transaction واحدة.
- حساب الوقت المتبقي على السيرفر من `started_at` ووقت النهاية، وليس من عداد المتصفح.
- حفظ الإجابة تلقائيًا عند الاختيار مع retry آمن في حال الشبكة غير المستقرة.
- استعادة الأسئلة والإجابات والوقت الصحيح عند إعادة الاتصال.
- تسليم تلقائي من worker عند انتهاء الوقت حتى لو أغلق الطالب المتصفح.

### 6.6 التصحيح والنتائج

- تصحيح كل سؤال موضوعي فور التسليم أو انتهاء الوقت.
- حساب الدرجة من `points_per_question` في slot الخاص بالسؤال لضمان ثبات الدرجة الكلية.
- حفظ `is_correct` و`points_awarded` و`final_score` و`graded_at`.
- تحديث إحصاءات الأسئلة بصورة غير متزامنة بعد التسليم حتى لا يتأخر الطلب.
- إتاحة تقرير النتائج للدكتور والمعيد المكلّف وAdmin فقط.

### 6.7 تصدير Excel

- يظهر خيار التصدير بعد إغلاق الامتحان فقط.
- ينشئ endpoint مهمة تصدير في queue بدل إنشاء الملف داخل طلب HTTP.
- ينشئ worker ملف Excel باستخدام ExcelJS.
- تكون الورقة الأولى مرتبة كالتالي:

```text
مادة: هندسة البرمجيات SWE301

اسم الطالب | الرقم الجامعي | الدرجة
```

- يمكن إضافة ورقة إدارية ثانية تتضمن حالة المحاولة ووقت التسليم وعدد المخالفات.
- حفظ الملف في تخزين داخلي آمن وتوفير رابط تنزيل للمستخدم المخول فقط، مع مدة صلاحية للملف.

### 6.8 المراقبة والمخالفات

- لوحة حية تعرض حالة الطلاب: لم يبدأ، جار، سلّم، وعداد المخالفات.
- تسجيل أحداث `COPY_PASTE` و`FULLSCREEN_EXIT` و`TAB_SWITCH` بتوقيت الخادم.
- استخدام fullscreen ومنع النسخ واللصق كرادع وتسجيل دليل، لا كضمان تقني كامل ضد الغش.

## 7. استراتيجية التحمل لعدد 10,000 طالب

- خوادم API stateless؛ لا تحفظ أي جلسة أو محاولة في ذاكرة instance واحد.
- استخدام PgBouncer لتجميع اتصالات PostgreSQL ومنع استنزافها.
- استخدام Redis Cluster أو Redis مع Sentinel على الأقل للحالة الحية وpub/sub والطوابير.
- تشغيل Socket.io على instances متعددة مع Redis adapter.
- عدم بث عداد الوقت كل ثانية لكل طالب؛ ترسل الواجهة timestamp من الخادم وتحسب العدّاد محليًا مع إعادة مزامنة دورية.
- استخدام WebSocket للأحداث المهمة والمراقبة فقط، وليس لحفظ كل إجابة.
- إنشاء `attempt_questions` بإدخال batch داخل transaction بدل صف واحد في كل استعلام.
- حفظ الإجابات في endpoint idempotent مع debounce خفيف في الواجهة وإعادة محاولة آمنة.
- تشغيل workers مستقلة لتصدير Excel والإحصاءات وإنهاء المحاولات المنتهية.
- مراقبة زمن الاستجابة واستهلاك قاعدة البيانات والذاكرة وRedis أثناء الاختبارات.

### معيار قبول الأداء

- محاكاة 10,000 مستخدم في سيناريوهات تسجيل الدخول وبدء الامتحان وحفظ الإجابات والتسليم وإعادة الاتصال.
- نجاح 95% من طلبات بدء المحاولة وحفظ الإجابة ضمن زمن استجابة متفق عليه، ويستهدف أقل من ثانيتين.
- عدم فقدان إجابات وعدم إنشاء محاولة مكررة وعدم وجود أخطاء 5xx غير مفسرة.
- تكرار الاختبار على عتاد الجامعة الفعلي قبل الإطلاق.

## 8. مراحل التنفيذ

| المرحلة | المخرجات | معيار الإنجاز |
|---|---|---|
| 1. تأسيس المشروع | Monorepo وDocker Compose وlint وtest | تشغيل API والواجهة وPostgreSQL وRedis محليًا |
| 2. قاعدة البيانات | Prisma schema وmigrations وseed data | إنشاء البيانات التجريبية بنجاح |
| 3. الهوية والصلاحيات | MockAuthProvider وJWT أو session وRBAC | دخول كل دور ومنع الوصول غير المصرح |
| 4. المقررات والطلاب | المقررات والهيئة والطلاب | ربط المستخدمين بالمادة وتصفية الموارد |
| 5. بنك الأسئلة | CRUD والتحقق والإحصاءات الأساسية | إدارة أسئلة المادة حسب الدور |
| 6. بناء الامتحان | exams وslots وفحص البنك والنشر | منع النشر عند نقص الأسئلة |
| 7. أداء الامتحان | توليد النسخة وsnapshot وautosave والتايمر | استئناف صحيح بعد انقطاع الاتصال |
| 8. التصحيح والنتائج | تسليم آمن ودرجات وتقارير | ظهور الدرجة للمخولين فقط |
| 9. Excel | queue وworker وملف درجات منظم | تنزيل ملف صحيح بعد إغلاق الامتحان |
| 10. المراقبة | Socket.io وسجل المخالفات | عرض الحالة والمخالفات لحظيًا |
| 11. الواجهة | شاشات كل دور وتصميم عربي متجاوب | رحلة كاملة قابلة للاستخدام |
| 12. الأمان والتشغيل | HTTPS وIP allowlist وbackup وrate limiting | تقييد واجهة الطالب لشبكة المعمل |
| 13. اختبار الحمل | سيناريوهات k6 وتحليل النتائج | اجتياز هدف 10,000 مستخدم |
| 14. SSO والنشر | مزود SSO الحقيقي وتوثيق النشر | اختبار قبول الجامعة والإطلاق الداخلي |

## 9. الاختبارات المطلوبة

### اختبارات الوحدة والتكامل

- اختيار الأسئلة حسب خلطة الصعوبة.
- فشل النشر عندما يكون بنك الأسئلة غير كافٍ.
- منع المحاولة المكررة.
- ثبات snapshot بعد تعديل السؤال الأصلي.
- صحة الوقت المتبقي بعد الاستئناف.
- صحة التصحيح والدرجة مع اختلاف الأسئلة.
- منع الطالب من قراءة النتائج أو endpoint التصدير.
- منع تعديل الامتحان بعد البدء.
- تسجيل audit log للتعديلات الإدارية.

### اختبارات نهاية إلى نهاية

1. طالب وهمي يسجل الدخول ويبدأ امتحانًا.
2. يجيب عن أسئلة وتُحفظ الإجابات تلقائيًا.
3. ينقطع الاتصال ثم يستأنف المحاولة نفسها.
4. يسلّم أو ينتهي الوقت، فيُصحح الامتحان.
5. الدكتور أو المعيد يرى النتيجة.
6. يغلق الامتحان ويطلب تصدير Excel وينزل ملف الدرجات.

### اختبارات الحمل

- استخدام k6 لتدرج الحمل حتى 10,000 مستخدم.
- اختبار موجة البدء في نفس النافذة الزمنية.
- اختبار حفظ إجابات متزامن.
- اختبار تسليم متزامن.
- اختبار إعادة اتصال جماعي.
- تحليل السجلات ومقاييس API وPostgreSQL وRedis قبل اعتماد النتيجة.

## 10. الأمان والتشغيل

- تقييد واجهة أداء الامتحان على نطاق IP معامل الجامعة.
- استخدام HTTPS داخلي وشهادات موثقة من البنية الداخلية عند الإمكان.
- تطبيق rate limiting على تسجيل الدخول وبدء المحاولة وحفظ الإجابات.
- الاحتفاظ بسجل تدقيق لا يمكن للمستخدم العادي تعديله.
- نسخ احتياطي مجدول لـPostgreSQL واختبار استرجاع فعلي.
- تحديد سياسة احتفاظ لسجلات المخالفات والـaudit log بالتنسيق مع الجامعة.
- استخدام متغيرات بيئة للأسرار وعدم حفظ كلمات مرور أو مفاتيح داخل المستودع.
- مراقبة الأخطاء والموارد والتنبيه عند ارتفاع زمن الاستجابة أو فشل worker.

## 11. نقاط معلقة قبل الإنتاج

- تأكيد بروتوكول SSO: SAML أو OAuth2 أو LDAP أو API خاص بالجامعة.
- معرفة العتاد المتاح في الجامعة لتحديد عدد خوادم API وموارد PostgreSQL وRedis.
- تحديد نطاقات IP الخاصة بالمعامل.
- الاتفاق على مدة الاحتفاظ بسجلات المخالفات وسجل التدقيق وملفات التصدير.
- اعتماد معايير نجاح اختبار الحمل قبل موعد الإطلاق.

## 12. ترتيب التنفيذ الفعلي الآن

1. إنشاء الهيكل الأساسي للمشروع وبيئة Docker للتطوير.
2. بناء Prisma schema وmigrations وseed data تجريبية.
3. تنفيذ تسجيل الدخول الوهمي والصلاحيات.
4. تنفيذ بنك الأسئلة والمقررات.
5. تنفيذ إنشاء ونشر الامتحان وخلطة الصعوبة.
6. تنفيذ مسار MVP كامل: بدء محاولة، حفظ إجابة، تسليم، تصحيح، عرض النتائج، تصدير Excel.
7. إضافة الاستئناف والتايمر والمخالفات والمراقبة الحية.
8. تنفيذ البيئة الموزعة واختبارات k6 وتحسين الأداء إلى الهدف.
9. ربط SSO الحقيقي وإجراء اختبار قبول قبل النشر الداخلي.
